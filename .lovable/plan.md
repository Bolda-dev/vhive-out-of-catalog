# Remove thumbnail from Equipment Type cell

In `src/components/out-of-catalog/OutOfCatalogTable.tsx`, simplify `EquipmentTypeCell` so it renders only the equipment type text — drop the `<img>` thumbnail and its placeholder fallback. The captures still appear in the Review Session hero/rail.
